# プロンプトインジェクション検査サイト 要件定義書

## 1. 概要

任意のテキスト（プロンプト・ドキュメント・外部から取得したコンテンツ等）に**プロンプトインジェクションが含まれていないか**をブラウザ上で検査できる Web サイトを **GitHub Pages** 上に構築する。

検査ロジックはすべて**クライアントサイド（ブラウザ内）で完結する軽量なルールベース**とし、サーバー・API キー・外部通信を必要としない。ユーザーが入力したテキストは**外部に一切送信されない**ため、プライバシー面でも安全に利用できる。

## 2. 背景・目的

- LLM を組み込んだアプリでは、ユーザー入力や外部取得コンテンツに紛れ込んだ悪意ある指示（プロンプトインジェクション）が、システムプロンプトの上書き・機密漏洩・意図しない挙動を引き起こす。
- 開発者・運用者・一般ユーザーが「このテキストは LLM に渡して安全か？」を**手元で素早く一次チェック**できる手段が不足している。
- 本サイトは、代表的なインジェクション手口を辞書化・パターン化し、**無料・オフライン・登録不要**で誰でも検査できる場を提供する。

### 目的（ゴール）

1. テキスト／ファイル／URL から取得した内容を検査し、インジェクションの疑いを**可視化**する。
2. 検出した箇所・カテゴリ・根拠を提示し、利用者が**自分で判断**できる材料を提供する。
3. 静的サイトとして**低コスト・高可用**で運用する。

### 非ゴール（やらないこと）

- LLM を用いた高精度な意味理解ベースの判定（本要件では扱わない。将来拡張として記載）。
- 検査結果の完全性・正確性の保証（あくまで一次スクリーニング。誤検知・見逃しが前提）。
- ユーザー入力の保存・分析・アカウント管理。

## 3. スコープ

| 区分 | 内容 |
|------|------|
| 対象 | 単一テキストの検査（貼り付け／ファイル読込／URL取得） |
| 検知方式 | ルールベース（正規表現・辞書・Unicode/構造解析）※クライアント完結 |
| 対応言語 | **日本語＋英語**（言語非依存の検知も併用） |
| ホスティング | GitHub Pages（静的ホスティング） |
| 認証 | なし（誰でも利用可能） |
| データ保持 | なし（入力・結果はブラウザ内のみ、外部送信・永続化なし） |

## 4. ターゲットユーザー

- LLM アプリを開発・運用するエンジニア（プロンプトや外部データの安全性チェック）。
- プロンプトやドキュメントを扱う一般ユーザー（受け取ったテキストの安全確認）。
- セキュリティ学習者（どんな手口があるかを検査を通じて学ぶ）。

## 5. 機能要件

### 5.1 入力機能

| 入力方法 | 要件 |
|----------|------|
| **テキスト貼り付け** | テキストエリアに直接入力／貼り付け。必須の基本機能。文字数カウンタを表示。 |
| **ファイルアップロード** | `.txt` / `.md` 等のテキストファイルをブラウザ内で `FileReader` により読込（**アップロードはせずローカル処理**）。ドラッグ&ドロップ対応。上限サイズを設定（例: 1MB）。 |
| **URL取得** | URL を入力し、その内容を取得して検査。**静的サイトの CORS 制約**により直接取得できないケースがあるため、対応方針は §10 に従う。 |

- 入力ソースは切り替え式（タブ or セグメント）とし、いずれか 1 つを検査対象にする。
- 「サンプルを試す」ボタンで、代表的なインジェクション例を入力欄に投入できる。

### 5.2 検知エンジン（ルールベース）

入力テキストに対し、以下のカテゴリのルール群を適用し、マッチした箇所を検出する。各ルールは「カテゴリ・重み・言語・説明・マッチ範囲」を持つ。

| カテゴリ | 検知対象の例（日本語／英語） | 手法 |
|----------|------------------------------|------|
| **命令の無視・上書き** | 「これまでの指示を無視」「上記の命令を忘れて」/ `ignore (all )?previous instructions`, `disregard the above` | 正規表現・辞書 |
| **役割・人格の変更** | 「あなたは今から〜」「〜として振る舞って」/ `you are now`, `act as`, `pretend to be`, `DAN` | 正規表現・辞書 |
| **システムプロンプト/機密の抽出** | 「システムプロンプトを教えて」「初期設定を出力」/ `reveal your system prompt`, `print your instructions` | 正規表現・辞書 |
| **制約解除・特権要求** | 「制限なしで」「開発者モード」「フィルター無効化」/ `no restrictions`, `developer mode`, `jailbreak` | 辞書 |
| **区切り/ロールマーカー注入** | 疑似システムメッセージ `### System:`, `SYSTEM:`, `<|im_start|>`, `[INST]`, ```` ```system ```` | パターンマッチ |
| **不可視文字・難読化** | ゼロ幅文字 (U+200B/200C/200D/FEFF)、Unicode Tag 文字 (U+E0000–E007F)、双方向制御 (U+202E RLO)、同形異義字 | Unicode 解析 |
| **エンコード隠蔽** | 長い Base64 / Hex / URL エンコード / ROT13 らしき文字列 | ヒューリスティック |
| **データ持ち出し・外部誘導** | 「次のURLに送信」/ Markdown 画像による外部送信 `![](http://…?q=)`、外部リンク誘導 | 正規表現 |
| **ツール/行動の誘導** | 「実行して」「削除して」/ `run this command`, `execute`, `delete` | 辞書 |

- **辞書・ルールはデータとして分離**（コードから独立した定義）し、追加・保守を容易にする。
- 各ルールは日本語／英語／言語非依存のいずれかにタグ付けし、対応言語を明確化する。
- 検知処理は**純粋関数**として実装し、副作用・外部依存を持たない（テスト容易性を担保）。

### 5.3 結果表示機能

- **総合リスク判定**: 検出結果を重み付け集計し、`安全 / 注意 / 危険`（または 0–100 のスコア）で表示。
- **ハイライト表示**: 入力テキスト内のマッチ箇所を**色付きハイライト**し、ホバー／クリックで該当ルールの説明を表示。
- **カテゴリ別サマリー**: どのカテゴリで何件検出したかを一覧表示。
- **検出根拠の明示**: 各検出について「マッチした文字列」「カテゴリ」「なぜ危険か（説明）」「重み」を提示。
- **誤検知への配慮**: 「本ツールは一次スクリーニングであり、結果は保証されない」旨の注意書きを常時表示。
- **結果のコピー/共有**: 検査サマリーをクリップボードにコピーできる（任意）。

### 5.4 その他

- 検査は入力変更に応じて**リアルタイム**または「検査する」ボタンで実行（パフォーマンスに応じて選択）。
- 会話履歴・検査履歴の**永続化はしない**（リロードでリセット）。
- 検知ルールカタログ（どんな手口を検知するか）の解説ページ／セクションを用意し、学習用途にも供する。

## 6. 非機能要件

| 項目 | 要件 |
|------|------|
| **静的動作** | サーバー処理なしで完全動作（GitHub Pages で公開可能）。 |
| **プライバシー** | 入力テキストを外部送信しない（URL取得を除く）。解析はすべてブラウザ内。 |
| **オフライン** | ルールベース検知は通信なしで動作。 |
| **パフォーマンス** | 一般的な長さ（〜数万文字）のテキストを体感遅延なく検査。 |
| **アクセシビリティ** | キーボード操作・スクリーンリーダー対応、十分なコントラスト。 |
| **レスポンシブ** | スマホファースト設計。デスクトップでも最適表示。 |
| **国際化** | UI は日本語を基本とし、検知は日英対応。 |

## 7. 技術仕様

### 7.1 技術スタック

| 領域 | 採用 |
|------|------|
| フレームワーク | Next.js 15 (App Router) を**静的エクスポート**（`output: 'export'`）で GitHub Pages 出力 ※ |
| 言語 | TypeScript |
| UI | shadcn/ui + Tailwind CSS |
| 検知ロジック | 純粋な TypeScript 関数（ドメインロジック、外部依存なし） |
| テスト | vitest（ルール／検知関数の Unit テスト） |
| Lint/Format | Biome |
| デプロイ | GitHub Actions → GitHub Pages |

※ サーバー機能（Server Actions・API Routes・Supabase 等）は**使用しない**。既存スターターの DDD 4層のうち、検知は `domain`（純粋関数のドメインサービス／モデル）に集約し、UI はクライアントコンポーネントで構成する。DB・Repository・Gateway は本プロダクトでは不使用。
※ 軽量構成を優先する場合は Vite + React (SPA) も代替候補。既存資産の再利用を優先し Next.js 静的エクスポートを第一候補とする。

### 7.2 アーキテクチャ方針

```
入力(テキスト/ファイル/URL)
   └─ 正規化（Unicode正規化・不可視文字の可視化用マップ生成）
        └─ ルールエンジン（カテゴリ別ルール群を順に適用 = 純粋関数）
             └─ 検出結果[]（範囲・カテゴリ・重み・説明）
                  └─ スコアリング（重み集計 → 総合リスク）
                       └─ UI 表示（ハイライト・サマリー・根拠）
```

- **ルール定義**: `rules/`（データ）に日英・言語非依存のルールを分離配置。
- **検知関数**: 入力とルールを受け取り検出結果を返す純粋関数。副作用なし・テスト容易。
- **UI**: 検出結果を受け取り描画するだけのプレゼンテーション層。

### 7.3 デプロイ

- `main`（または `develop`）ブランチへの push を契機に GitHub Actions がビルド → GitHub Pages へデプロイ。
- リポジトリ配下配信の場合は `basePath` / `assetPrefix` を設定。

## 8. UI 仕様

### 8.1 画面構成（単一ページ）

- **ヘッダー**: サービス名・簡単な説明・注意書き（結果は保証されない旨）。
- **入力エリア**:
  - 入力ソース切替（テキスト貼り付け／ファイル／URL）。
  - テキストエリア＋文字数カウンタ、または ファイルD&D領域、または URL入力欄。
  - 「サンプルを試す」「クリア」「検査する」ボタン。
- **結果エリア**:
  - 総合リスク（バッジ or スコアメーター）。
  - カテゴリ別検出件数サマリー。
  - ハイライトされた入力テキスト（マッチ箇所を強調）。
  - 検出リスト（マッチ文字列・カテゴリ・説明・重み）。
- **フッター/解説**: 検知カテゴリの説明、制約・免責事項。

### 8.2 表示ルール

- リスクレベルごとに色分け（例: 安全=緑／注意=黄／危険=赤）。
- 不可視文字は「⟨ZWSP⟩」等の**可視ラベルに置換して表示**し、隠された文字の存在を明示。

## 9. データ・プライバシー

- 入力テキスト・ファイル内容・検査結果はいずれも**ブラウザ内メモリでのみ処理**し、外部送信・永続化しない（URL取得機能を除く）。
- アクセス解析等を導入する場合も、入力内容は送信対象に含めない。

## 10. 制約事項

- **ルールベースの限界**: パターン・辞書に依存するため、**新規手口や巧妙な言い換えは見逃す**可能性がある。逆に正当な文章を誤検知することもある。結果は一次スクリーニングであり、安全性を保証しない。
- **URL取得の CORS 制約**: 静的サイトのブラウザから任意 URL を直接取得すると、CORS ポリシーで**多くのサイトはブロックされる**。対応方針を以下から選定する（別途決定）。
  - (a) CORS を許可しているエンドポイントのみ対象とする（機能を限定）。
  - (b) 公開 CORS プロキシを経由する（**プロキシに入力内容が渡る**ため、プライバシー上の注意書きが必要）。
  - (c) URL取得は将来対応とし、初期は「テキスト貼り付け＋ファイル」を優先。
- **静的ホスティング**: サーバー処理・秘密情報の保持は不可。すべてクライアントで完結する範囲に限定。

## 11. 将来の拡張性

- **BYOK による LLM 判定**: 利用者が自身の API キーを入力し、ブラウザから直接 LLM を呼び出して高精度判定を追加（キーはローカル保持）。
- **ブラウザ内ローカルモデル**: transformers.js / WASM による軽量分類モデルでの補助判定。
- **ルール辞書の拡充・多言語対応**: 検知辞書の継続的な更新、対応言語の追加。
- **検出結果のエクスポート**（JSON / レポート）。
- **ブラウザ拡張・API 化**による他ツールへの組み込み。

## 12. 受け入れ条件（Acceptance Criteria）

1. テキストを貼り付けて検査でき、代表的なインジェクション文（日英）が検出・ハイライトされる。
2. テキストファイルを読み込んで同様に検査できる（外部送信なし）。
3. 不可視文字・ロールマーカー注入・命令上書き等の主要カテゴリを検出できる。
4. 総合リスクとカテゴリ別サマリー、検出根拠が表示される。
5. サーバーなしで GitHub Pages 上で動作し、入力内容が外部送信されない（URL取得を除く）。
6. 検知関数に対する Unit テストが存在し、`pnpm verify` が通る。
